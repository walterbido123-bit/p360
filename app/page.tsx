import { StoryCard } from '@/components/StoryCard';
import { categories, stories, byCategory } from '@/lib/content';
export default function Home(){const lead=stories.find(s=>s.featured)??stories[0];return <div className="container"><section className="hero" aria-labelledby="portada"><div><span className="eyebrow">Portada 360°</span><h1 id="portada">Información para entender el día, no solo para seguirlo</h1><p>Una nueva experiencia editorial preparada para noticias en tiempo real, periodismo asistido por IA y cobertura multimedia.</p></div><StoryCard story={lead} large/></section><section className="latest"><div className="sectionTitle"><h2>Últimas noticias</h2><span>Actualidad en perspectiva</span></div><div className="grid3">{stories.slice(1,4).map(s=><StoryCard key={s.id} story={s}/>)}</div></section>{categories.map(c=>{const items=byCategory(c);if(!items.length)return null;return <section key={c} className="categoryBlock"><div className="sectionTitle"><h2>{c}</h2><a href={`/categoria/${encodeURIComponent(c.toLowerCase())}`}>Ver sección →</a></div><div className="grid3">{items.slice(0,3).map(s=><StoryCard key={s.id} story={s}/>)}</div></section>})}<section className="newsletter"><div><span className="eyebrow">Newsletter 360</span><h2>Las claves del día, directamente en tu correo</h2></div><form><label className="srOnly" htmlFor="email">Correo electrónico</label><input id="email" type="email" placeholder="tu@email.com"/><button type="submit">Suscribirme</button></form></section></div>}
